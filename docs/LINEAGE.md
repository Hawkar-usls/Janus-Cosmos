# Technical lineage

`PR190 generators → PR192 corrected six-direction mechanics → OSIRIS v2 content-bound SAT solver → OSIRIS v2.1 verified Q0 quotient → S𓂸ḥ/0 K1 articulation → ORIGIN_PRIME ribbon state → S𓂸ḥ/1 K2 separator → S𓂸ḥ/2 automatic bounded-K holdout`.

All source files, frozen contracts, failed runs and historical operator implementations remain reachable through the exact `vendor/Janus-Fundamentum` pin. All registry JSONs are reachable through `receipts` / `vendor/janus-meta-registry`.

The next native research gate is `S𓂸ḥ/3`: broader out-of-family graph/CNF holdout and K-growth beyond fixed `K_MAX=4`, while attacking the minimum-separator discovery cost rather than hiding it.
