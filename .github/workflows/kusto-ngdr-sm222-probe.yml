name: KUSTO NGDR SM222 Endpoint Probe
on:
  push:
    branches: [feature/kusto-global-groundtruth-calibration-v1]
    paths:
      - 'workspace/kusto_ngdr_sm222_probe.py'
      - '.github/workflows/kusto-ngdr-sm222-probe.yml'
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install --disable-pip-version-check requests
      - run: python workspace/kusto_ngdr_sm222_probe.py
      - uses: actions/upload-artifact@v4
        with:
          name: kusto-ngdr-sm222-endpoint-probe-v1
          path: workspace/kusto_global_groundtruth_out/ngdr_sm222_probe/
          retention-days: 30
