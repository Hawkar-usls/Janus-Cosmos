name: GOLDMEMBER Waiting Lanes Council

on:
  workflow_dispatch:
  push:
    branches: [janus-echo-cousteau]
    paths:
      - "workspace/goldmember_waiting_lanes_council.py"
      - ".github/workflows/goldmember-waiting-lanes-council.yml"

permissions:
  contents: write

concurrency:
  group: goldmember-waiting-lanes-${{ github.ref }}
  cancel-in-progress: false

jobs:
  council:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: janus-echo-cousteau
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Ask JANUS what public falsification work is allowed while custodians are pending
        run: python workspace/goldmember_waiting_lanes_council.py
      - name: Show council answer
        run: cat data/cousteau/GOLDMEMBER-WAITING-LANES-COUNCIL-RUN-008-2026-08-22-v1.0.json
      - uses: actions/upload-artifact@v4
        with:
          name: goldmember-waiting-lanes-council-${{ github.run_number }}
          path: data/cousteau/GOLDMEMBER-WAITING-LANES-COUNCIL-RUN-008-2026-08-22-v1.0.json
          retention-days: 90
      - name: Freeze council result safely
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/cousteau/GOLDMEMBER-WAITING-LANES-COUNCIL-RUN-008-2026-08-22-v1.0.json
          git commit -m "GOLDMEMBER: freeze waiting-lanes council ${{ github.run_number }}" || true
          git fetch origin janus-echo-cousteau
          git rebase origin/janus-echo-cousteau
          git push origin HEAD:janus-echo-cousteau
