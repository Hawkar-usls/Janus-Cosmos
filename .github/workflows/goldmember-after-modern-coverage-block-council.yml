name: GOLDMEMBER After Modern Coverage Block Council

on:
  workflow_dispatch:
  push:
    branches: [janus-echo-cousteau]
    paths:
      - "workspace/goldmember_after_modern_coverage_block_council.py"
      - ".github/workflows/goldmember-after-modern-coverage-block-council.yml"

permissions:
  contents: write

concurrency:
  group: goldmember-after-modern-coverage-block-${{ github.ref }}
  cancel-in-progress: false

jobs:
  council:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: janus-echo-cousteau
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Ask JANUS how to resolve blocked HI1751 coverage gate
        run: python workspace/goldmember_after_modern_coverage_block_council.py
      - name: Show council answer
        run: cat data/cousteau/GOLDMEMBER-AFTER-MODERN-COVERAGE-BLOCK-COUNCIL-RUN-009-2026-08-22-v1.0.json
      - uses: actions/upload-artifact@v4
        with:
          name: goldmember-after-modern-coverage-block-${{ github.run_number }}
          path: data/cousteau/GOLDMEMBER-AFTER-MODERN-COVERAGE-BLOCK-COUNCIL-RUN-009-2026-08-22-v1.0.json
          retention-days: 90
      - name: Freeze council result safely
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/cousteau/GOLDMEMBER-AFTER-MODERN-COVERAGE-BLOCK-COUNCIL-RUN-009-2026-08-22-v1.0.json
          git commit -m "GOLDMEMBER: freeze post-modern-coverage-block council ${{ github.run_number }}" || true
          git fetch origin janus-echo-cousteau
          git rebase origin/janus-echo-cousteau
          git push origin HEAD:janus-echo-cousteau
