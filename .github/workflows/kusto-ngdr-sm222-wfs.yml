name: KUSTO NGDR SM222 WFS Point In Polygon
on:
  push:
    branches: [feature/kusto-global-groundtruth-calibration-v1]
    paths:
      - 'workspace/kusto_ngdr_sm222_wfs.py'
      - '.github/workflows/kusto-ngdr-sm222-wfs.yml'
  workflow_dispatch:
jobs:
  q:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install --disable-pip-version-check requests
      - run: python workspace/kusto_ngdr_sm222_wfs.py
      - uses: actions/upload-artifact@v4
        with:
          name: kusto-ngdr-sm222-wfs-v1
          path: workspace/kusto_global_groundtruth_out/ngdr_sm222_wfs/
          retention-days: 30
